# Lobby webroot seed (install bundle)

HTML/JS casino wrappers only — **no** `.swf` (copyright; upload via Admin → Casino).

Copied to `data/webroot/` on first install when `casino/slot/index.html` is missing.
