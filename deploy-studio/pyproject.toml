[project]
name = "smt-deploy-studio"
version = "0.1.0"
description = "Portable Path 1 armory portrait studio (Wine client + queue HTTP)"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0.0",
  "pillow>=10.0.0",
]

[tool.uv]
package = false
