<objects>
    <!--
      DEFERRED (Phase 10 client compressors patch + Phase 17A true custom item).

      Magical Golden Apple 21941 -> Compressed 900003 @ 50000 was prototyped here,
      but the Note/Presser yes/no dialog is client-hardcoded for those item IDs,
      and the Mag-Presser-clone compressed item failed client inventory rendering
      (invisible slots). Keep AutoCompress for stock Macca/Mag only until then.

      When re-enabled, restore:
        ID 900001 / BaseItemID 21941 / CompressedItemID 900003 / Value 50000
      and ship a real icon + client compressor config (not RandomItem alone).
    -->
</objects>
