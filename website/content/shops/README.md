# Shop product extract (Phase 16C)

`shop-products.json` maps ShopProductData `ProductID` → item id, stack, CP
flag, and display name. Regenerate with:

```bash
./scripts/shop-export-products.sh
```

from `ai_custom_smt_server/`.
