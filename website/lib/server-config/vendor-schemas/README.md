# Vendored from comp_hack for Docker/Hub website images (objgen lobby/world/channel editors).
# Source paths:
#   libcomp/libcomp/schema/{server,database}config*.xml
#   server/{lobby,world,channel}/schema/*config.xml
# Refresh when those schemas change in comp_hack.
