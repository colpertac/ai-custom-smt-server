[project]
name = "smt-portrait"
version = "0.1.0"
description = "Path 1 armory portrait worker (Wine client + studio + queue HTTP)"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0.0",
  "pillow>=10.0.0",
]

[tool.uv]
package = false
